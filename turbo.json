{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "ANTHROPIC_API_KEY",
    "DATABASE_URL",
    "DEEPGRAM_API_KEY",
    "DEEPSEEK_API_KEY",
    "GROQ_API_KEY",
    "LOGTO_APP_ID",
    "LOGTO_APP_SECRET",
    "LOGTO_BASE_URL",
    "LOGTO_COOKIE_SECRET",
    "LOGTO_ENDPOINT",
    "LOGTO_ISSUER",
    "LOGTO_JWKS_URI",
    "LOGTO_M2M_APP_ID",
    "LOGTO_M2M_APP_SECRET",
    "SUPABASE_ACCESS_TOKEN",
    "SUPABASE_ANON_KEY",
    "SUPABASE_DB_PASSWORD",
    "SUPABASE_ORG_ID",
    "SUPABASE_PROJECT_REF",
    "SUPABASE_PROJECT_URL",
    "SUPABASE_SERVICE_ROLE_KEY",
    "TIKHUB_API_KEY",
    "TRIGGER_PROJECT_REF",
    "TRIGGER_SECRET_KEY",
    "YOUTUBE_API_KEY"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      // .next/dev is dev-server state, not build output, but it sits under .next — so once
      // `pnpm dev` has run, every build snapshotted its 2.4GB Turbopack cache into .turbo.
      // That is how the local cache reached 38GB across 2013 entries.
      "outputs": [".next/**", "!.next/cache/**", "!.next/dev/**", "dist/**"]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "clean": {
      "cache": false
    }
  }
}
